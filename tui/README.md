# Apple Music CLI — TUI

Bubble Tea v2 기반 터미널 클라이언트.

## 레이아웃 — Apple Music 과 같은 모양

```
┌─────────────────────────────────────┐
│ ▶ 지금 재생 중 ── 진행 바 ── 1:22/4:22 │  ← 화면의 첫 줄
├──────────┬──────────────────────────┤
│ 사이드바  │  목록 (최대 14줄)          │
├──────────┴──────────────────────────┤
│ 근거 / Thinking… / 실패 (있을 때만)   │
│ 입력창 (자연어 · / 명령 · 검색)        │
│ 상태줄 (큐 요약 · 누적 사용량)         │
└─────────────────────────────────────┘
```

**선정 근거 줄은 의도 층이 만든 곡에서만 나타난다.**
사람이 목록에서 직접 고른 곡에는 근거가 없으므로 줄 자체가 없다.
빈 줄을 남기지 않는다.

**섹션 제목과 곡 수는 두지 않는다.** 사이드바의 레일이 이미 어디인지 말해주고,
그 자리는 지금 재생 중인 곡에 준다.

**목록은 14줄까지만 보여준다.** 화면을 꽉 채우면 읽을 것이 아니라
스캔할 것이 되어버린다.

**사이드바가 무엇을 고르든 목록 패널 하나가 다 그린다.**
Recently Added · Artists · Albums · Songs · 플레이리스트 · Queue 가 전부
같은 패널이므로 화면이 늘어나지 않는다.

## 환경 변수

의도 층에 `OPENAI_API_KEY` 가 필요하다. 저장소 루트의 `.env` 를 읽는다.

```
set -a && . ./.env && set +a && cd tui && go run .
```

## 첫 실행 — 로그인은 없다

Music.app 이 이미 사용자 Apple ID 로 로그인되어 있으므로 **별도 로그인이 없다.**
대신 관문이 둘이다.

| 관문 | 화면 | 해결 |
|---|---|---|
| **macOS 자동화 권한** (E1) | `PERMISSION` 배지 | `ctrl+g` 로 시스템 설정 열기 |
| **Music.app 미실행** (E2) | `MUSIC APP` 배지 | `open -a Music` |

둘 다 통과하면 그 줄은 다시 보이지 않는다.

## 실행

```
go run .                  실제 TUI
go run ./cmd/render 96    헤드리스 렌더 (TTY 없이 화면 확인)
go run ./cmd/queue "1시간 코딩용 큐"   의도 층만 확인
go run ./cmd/flow  "30분 조용한 걸로"  프롬프트→큐 전체 흐름 확인
go test ./...             폭 계산 · 필수 요소 검증
```

## 조작

**입력창은 늘 활성이다.** 포커스 개념이 없으므로 언제든 바로 칠 수 있다.

### 입력창은 하나지만 하는 일이 둘이다

무엇을 하는 중인지 **기호와 사이드바가 말해준다.**

| 모드 | 기호 | 목록 | Enter |
|---|---|---|---|
| **프롬프트** (기본) | `›` | **건드리지 않는다** | 요청으로 보낸다 |
| **검색** (`ctrl+f`) | `⌕` | **즉시 걸러진다** | 고른 곡을 튼다 |
| 명령 (`/` 로 시작) | `›` | 건드리지 않는다 | 로컬 실행 |

검색 중에는 사이드바 맨 위에 `▌ ⌕ Search` 가 뜨고 섹션 선택 표시가 꺼진다.
**목록이 왜 걸러졌는지가 화면에 설명되어 있다.**

| 키 | 동작 |
|---|---|
| 아무 글자 | 입력창에 들어간다 (프롬프트) |
| `ctrl+f` | 검색 모드 |
| `shift+←` `shift+→` | 이전 / 다음 곡 |
| `ctrl+g` | (권한 막혔을 때) 시스템 설정 열기 |
| `↑` `↓` (`ctrl+p` `ctrl+n`) | 목록 이동 |
| `tab` / `shift+tab` | 사이드바 섹션 이동 |
| `enter` | **프롬프트면 큐를 만들고 첫 곡을 튼다.** 검색이면 고른 곡 재생 |
| `esc` | 검색 나가기 → 입력 비우기 → 종료 |
| `ctrl+c` | 종료 |

`j` `k` `h` `l` 같은 알파벳 단축키는 두지 않는다.
입력창이 늘 활성인 구조에서는 자연어와 반드시 충돌한다.

## 인식 — `/shazam`

마이크로 몇 초 듣고 무슨 곡인지 말한다. 알아맞힌 곡이 **이미 라이브러리에
있는지**를 함께 말하는 것이 이 서비스가 Shazam 보다 더 하는 일이다.

ShazamKit 은 Go 에서 부를 수 없어 옆에 Swift 헬퍼를 세운다. 먼저 만들어야 한다.

```
make -C ../helper
```

카탈로그 대조에는 Apple 의 ShazamKit 엔타이틀먼트가 필요하고, 그것만은
개발자 포털에서 한 번 받아와야 한다 — `helper/README.md`. 없으면 `/shazam`
이 왜 안 되는지 로그에 적고 나머지 기능은 그대로 돈다.

## 구조

```
main.go                    진입점
cmd/render/                헤드리스 렌더
internal/api/types.gen.go  ★ api/openapi.yml 에서 자동 생성 (oapi-codegen)
internal/music/music.go    ★ Music.app 제어 (AppleScript)
internal/intent/           ★ 자연어 한 줄 → 큐 (gpt-5.5, 구조화 출력)
internal/data/
  library.json             ★ 실제 Music.app 라이브러리 157곡
  library.go               조회 · 정렬 · 검색
  dummy.go                 의도 층이 만든 재생 큐 (고정값)
internal/ui/
  model.go     루트 모델 · 레이아웃 · 키 처리
  sidebar.go   사이드바
  list.go      목록 패널 (곡 · 아티스트 · 앨범 공용)
  player.go    재생 바 · 선정 근거
  poll.go      1초 폴링 · 재생 명령
  layout.go    폭 계산 · 진행 바 · 시간 포맷
  theme.go     팔레트
```

## 타입은 손으로 쓰지 않는다

`internal/api/types.gen.go` 는 API 명세에서 생성된다.

```
go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@latest \
  -config oapi-codegen.yaml ../api/openapi.yml
```

명세에 없는 필드를 화면이 그리려 하면 컴파일이 안 된다.

## 진행 상황

| | 상태 |
|---|---|
| 레이아웃 뼈대 (사이드바 · 목록 · 재생 바) | ✅ |
| Recently Added · Songs | ✅ |
| Artists · Albums · Playlists | ✅ |
| Queue | ✅ |
| 검색 (입력창 타이핑) | ✅ |
| 재생 제어 실연동 (AppleScript) | ✅ |
| 첫 실행 관문 (권한 · 앱 실행) | ✅ |
| **자연어 의도 → 선곡 → 재생** | ✅ |
| Shazam 인식 (`/shazam` · 헬퍼 · 대조) | ✅ — 엔타이틀먼트 대기 |
| 취향 규칙 화면 | — |
| 반응 수집 (스킵 관찰) | — |

## 알려진 제약

Bubble Tea 는 셀 기반 렌더러라 kitty graphics / iTerm2 inline image 를
쓸 수 없다. 앨범 아트는 현재 화면에서 뺐다 (반블록 렌더링으로 넣는 것은 가능).
