# Apple Music CLI — 환경 변수
# 이 파일을 .env 로 복사한 뒤 값을 채우세요. .env 는 git 에 올라가지 않습니다.

# 필수 — 의도 층(자연어 → 큐)과 선곡 근거. internal/intent 가 씁니다.
OPENAI_API_KEY=

# 선택 — Apple Music 카탈로그 (F8 유사곡 탐색)
# Apple Developer 의 Media Services 키. 없으면 라이브러리 안에서만 찾습니다.
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY_PATH=
