# Relay — 환경 변수 템플릿
# 이 파일을 .env 로 복사한 뒤 실제 값을 채우세요. (.env 는 git에 올라가지 않습니다)

# 필수 — 임베딩 / 생성 모델
OPENAI_API_KEY=sk-...

# 선택 — GitHub 지식 소스
# 공개 레포는 토큰 없이도 됩니다 (시간당 60회 제한).
# 토큰이 있으면 시간당 5,000회로 늘어납니다. 읽기 전용 권한이면 충분합니다.
GITHUB_TOKEN=
GITHUB_REPO=supabase/supabase
