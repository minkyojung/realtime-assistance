# 환경 변수 / 시크릿
.env
.env.*
!.env.example

# Conductor 작업 디렉토리
.context/

# OS
.DS_Store

# Go 빌드 산출물
spikes/**/hangul-spike
*.test
tui/amcli

# Swift 헬퍼 빌드 산출물
helper/build/

# 배포 바이너리 — scripts/build.sh 가 만든다
dist/

# 제출용으로 복사한 것. 원본은 api/ · db/ 에 있다
제출/*.pdf
